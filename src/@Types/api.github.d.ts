export type PropsCardBox = {
    id?: number
    repoTitle: string
    vercelURL?: string
    repoDescription: string
    amountStars: number
    language: string
    lastEdition: string | date | number
    githubURL: string
}